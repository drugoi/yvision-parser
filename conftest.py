# Present so that running `pytest` from the repo root puts this directory on
# sys.path, allowing the tests to import the flat top-level modules
# (main, resolver, fetcher, ...) without an installable package.
